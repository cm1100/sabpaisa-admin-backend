"""
Dashboard Models - No new models needed, uses existing tables
Single Responsibility: Dashboard only aggregates existing data
"""
# Dashboard uses existing models from other apps
# This follows Interface Segregation - dashboard doesn't own data, just reads it